---
description: Show current config values — pricing, thresholds, hints, data paths. Verify overrides from config.env are loading.
---

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/dispatch.py config
```
